Version 1.0

Besides this README, this directory includes the following files:

- a Makefile for the project

- shell.h and shell.c, shell actions 

- cmd.c and cmd.h that introduces a (to simple) data structure for commands, and navigates between them.

- validate.c and validate.h that check a functions validity

- operations.c and operations.h that will link the operations to actual actions. 

- in the examples folder is a runnable version of the cli, type "make test" in the cli directory
 in order to compile an executable.