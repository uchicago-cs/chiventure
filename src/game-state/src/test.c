#include "game.h"
#include "player.h"
#include "room.h"
#include "item.h"

//hardcoded game struct
int main() {
    player_t *playerone = malloc(sizeof(player_t));
    player_init(playerone, 100);
    

}

