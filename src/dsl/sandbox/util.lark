string: STRING -> string
      | ESCAPED_STRING -> escaped_string

//STRING: (LETTER|DIGIT)+
PUNCTUATION: /[^\s\[\]\"\#]/
STRING: (LETTER|DIGIT|PUNCTUATION)+
ESCAPED_INNER: (LETTER|DIGIT|WS|PUNCTUATION)+
ESCAPED_STRING: "\"" ESCAPED_INNER "\""

%import common.LETTER
%import common.DIGIT
%import common.WS

COMMENT: "#" /[^\n]/*
