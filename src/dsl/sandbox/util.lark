STRING: (LETTER|DIGIT)+
PUNCTUATION: /[^\s\[\]\"\#]/
ESCAPED_INNER: (LETTER|DIGIT|NEWLINE|WS|PUNCTUATION)+
ESCAPED_STRING: "\"" ESCAPED_INNER "\""

%import common.LETTER
%import common.NEWLINE
%import common.DIGIT
%import common.WS

COMMENT: "#" /[^\n]/*
